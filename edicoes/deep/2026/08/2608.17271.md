---
id: paper-deep-2608.17271
title: "ASI-Bench: At the Dawn of Artificial Superintelligence"
type: paper-deep
date: 2026-08-19
arxiv: "2608.17271"
status: distilled
tags: [papers, deep, hugging-face]
relates-to:
  - "[[papers/2026/08/2026-08-19]]"
  - "[[papers/repos-registry]]"
---

# ASI-Bench: At the Dawn of Artificial Superintelligence

**Sumário de ação**: Vale destilar pelo desenho experimental, nunca pelo nome — nada aqui mede superinteligência, e o título é a parte mais fraca do trabalho. O que se rouba é a retirada progressiva de orientação *dentro da mesma tarefa* (B1→B4) e o achado que contraria a leitura fácil: o desabamento acontece quando some o procedimento passo a passo, não quando some a escolha do método. Escolher o caminho custa quase nada; transformar um método em procedimento executável custa 21,82 pontos.

> [!info] Fonte
> [abstract](https://arxiv.org/abs/2608.17271) · [HTML](https://arxiv.org/html/2608.17271) · 62 upvotes na edição de [[papers/2026/08/2026-08-19|2026-08-19]] · avança [[papers/deep/2026/08/2608.11341|17/08]]

## Afirmação central

Variar o grau de orientação metodológica dentro da mesma tarefa de pesquisa — em vez de comparar tarefas diferentes — isola quanto do desempenho de um agente é dele e quanto é do plano que lhe deram; feito isso, o que sobra é pouco.

## Mecânica passo a passo

**O gradiente B1–B4, que é a contribuição real.** Quatro condições sobre a *mesma* tarefa, com o insumo encolhendo:

- **B1** — orientação metodológica completa: equações governantes, formulação numérica e procedimento do solver entregues.
- **B2** — só a classe do método é nomeada; o agente tem de "turn this information into a working solution".
- **B3** — o agente recebe apenas os dados observados, o objetivo científico e as saídas exigidas. Tem de "determine the underlying model, choose an appropriate numerical method, implement it, and validate".
- **B4** — o B3 acrescido de informação plausível e irrelevante, para testar manutenção de direção sob distração.

Manter a tarefa fixa e mexer só no insumo é o que separa autonomia de dificuldade. Um benchmark que variasse orientação trocando de tarefa mediria as duas coisas somadas.

**Construção.** 60 tarefas de nível projeto em 11 domínios científicos (matemática, física, química, biologia, astronomia, materiais, ciências da terra, medicina e bioestatística, computação, robótica, engenharia elétrica). Mais de 40 especialistas, 31.000+ horas humanas declaradas, 1.300+ ideias candidatas coletadas, cinco rodadas de revisão com 1.100+ atribuições, 2.000+ revisões de tarefa, 1.500+ execuções em sandbox. 21 pesquisadores tiveram tarefas retidas no benchmark final.

**Pontuação.** Aqui o paper é vago e isso importa. Não há rubrica única: cada tarefa carrega o seu próprio scorer, com "validity checks and weighted scoring criteria", validado por execução ponta a ponta em sandbox contra uma solução de referência ("Every task is executed end-to-end in a sandbox environment to verify its reference solution and scoring behavior"). Tarefas com inconsistência científica, avaliação não confiável ou atalho não intencional são revisadas ou excluídas. **O paper não declara se a nota final sai de checagem automática, juiz-LLM ou humano** — descreve gates automáticos combinados com referência validada por humano, e para por aí. Quem escreveu a tarefa também escreveu o scorer dela: o instrumento de medida e o objeto medido saem da mesma mão, com auditoria assistida por IA e quatro rodadas de revisão cruzada como mitigação.

**Escala de execução.** As 60 tarefas somam mais de 2.600 turnos de interação e 2.400 passos de execução, mais de 35 horas de execução de agente.

## Evidência e resultados

**18 configurações agente×modelo.** Três harnesses nomeados (Codex, Claude Code, OpenHands) sobre uma dúzia de backbones: GPT-5.5 e GPT-5.6 Sol (xhigh e ultra), Claude Opus 5 e Opus 4.8, Kimi K3 e K2.7, GLM-5.3 e GLM-5.2, DeepSeek V4 Flash e V4 Pro, MiniMax M3, MiMo V2.5 Pro.

**As médias, que são o resultado do paper:**

| | B1 | B2 | B3 | B4 |
|---|---|---|---|---|
| Média das 18 configurações | 50,91 | 29,10 | 26,62 | 26,99 |
| Codex + GPT-5.6 Sol (ultra) | 71,78 | 49,57 | 51,60 | 50,41 |
| Claude Opus 5 | 72,29 | 45,80 | 40,70 | 42,39 |

**O achado que contraria a leitura fácil.** O abstract fala em "sharp decline" ao longo de B1→B2→B3, mas os números têm forma muito específica: a queda de B1 para B2 é de **21,82 pontos**; de B2 para B3 é de **2,48**; de B3 para B4 é *positiva* (+0,37). Os próprios autores dizem, com todas as letras: *"The main bottleneck is not method selection itself, but the ability to turn a scientific method into a complete research procedure"*, e que os sistemas se beneficiam "far more from step-by-step methodological guidance than from being told which method to use".

Isso corrige o enquadramento do destaque da edição. Não é que o modelo desabe quando precisa **escolher o caminho sozinho** — escolher o método é quase de graça. Ele desaba quando perde o **procedimento**: a decomposição em passos executáveis, a formulação numérica, a ordem das operações. A dependência que o benchmark isola é de *operacionalização*, não de *decisão*.

**Onde é forte.** O desenho da retirada de orientação sobre tarefa fixa é limpo e replicável, e a magnitude do efeito B1→B2 (21,82 pontos sobre uma média de 50,91 — mais de 40% do desempenho) é grande demais para ser artefato. O achado de que o harness carrega parte da capacidade também é bem colocado: *"Scientific capability cannot be attributed to the backbone model alone; it emerges from the interaction between the model and the harness"*, e *"Comparing models without accounting for their harnesses can obscure where capability actually comes from"*. Três harnesses cruzados com uma dúzia de modelos é o que dá direito a essa afirmação.

**Onde é fraco.** Média de três execuções independentes com desvio-padrão reportado — exceto Claude Opus 5, que é **execução única**, sem barra de erro, e ainda assim aparece na tabela ao lado dos que têm. O desvio reportado para GPT-5.6 Sol (xhigh) em B3 é ±2,35. A diferença B2→B3 da média geral é 2,48 pontos: está **dentro da banda de ruído** de uma única configuração. E a configuração mais forte pontua *mais* em B3 (51,60) do que em B2 (49,57), o que é incoerente com um gradiente monotônico de dificuldade. A conclusão defensável do paper é a queda B1→B2. B2 vs. B3 vs. B4 não se distingue do ruído com os dados publicados.

## Limitações e riscos

**O nome excede a evidência, e é a coisa mais importante a dizer sobre este paper.** "Artificial superintelligence" aparece no título e na primeira frase do abstract, definida como um *requisito* — "ASI requires AI to move beyond mastering existing knowledge toward exploring the unknown, creating new knowledge, and turning new ideas into verifiable results" — e nunca como uma *medida*. Não há definição operacional de superinteligência, não há limiar declarado, não há argumento de validade ligando a nota do benchmark a qualquer coisa que o nome sugira. O que o instrumento mede é: quanto uma configuração agente×modelo pontua em 60 tarefas de pesquisa quando se tira o passo a passo. Isso é um benchmark de execução de pesquisa assistida sob orientação decrescente — nome honesto e chato. Chamá-lo de ASI-Bench é escolha de marketing, e a moldura não deve ser adotada.

Vale registrar que o corpo do paper é mais sóbrio que a capa: a conclusão que ele defende é que os sistemas *continuam longe* de pesquisa autônoma. O título aponta para o amanhecer da superinteligência; o resultado é que 26,62 de 100 é o que se consegue sem receita. O paper que se lê contradiz o paper que se anuncia.

**Declarado (espalhado; não há seção "Limitations").**
- Benchmark longe de saturado, desempenho atual baixo.
- Nem a configuração mais forte chega perto: B3 de 51,60.
- Capacidade não é atribuível ao backbone; emerge de modelo × harness.
- Dois "primeiros" reivindicados no abstract ("the first benchmark to jointly evaluate...", "the first to progressively withdraw...") sem tabela comparativa que os sustente no que foi lido.

**Suspeitado pela leitura.**
- Scorer por tarefa escrito por quem escreveu a tarefa. A validação é procedural (rodadas de revisão, sandbox) e não estatística: nenhuma medida de concordância entre avaliadores, nenhuma correlação entre scorers de tarefas diferentes. Sem isso, "média 26,62 em 11 domínios" agrega 60 escalas que ninguém provou comensuráveis.
- Uma execução para Claude Opus 5 numa tabela de médias±desvio é comparação desigual apresentada como igual.
- Benchmark hospedado pelos autores, com portal aberto de submissão de tarefas. Leaderboard próprio, tarefas de terceiros, critério de aceitação não descrito no que foi lido.
- 31.000 horas humanas é número de esforço, não de qualidade. Aparece no abstract onde normalmente iria uma medida de confiabilidade.
- B4 (distração) pontua acima de B3 na média. Ou a distração não distrai, ou a diferença toda é ruído — as duas leituras enfraquecem a condição.

## Teses transferíveis

1. **Executar plano dado e formular o plano são duas capacidades, e precisam de dois gates.** Um pipeline que só valida a entrega contra um plano aprovado mede B1 para sempre, e o número que produz não diz nada sobre trabalho aberto. Aprovar no gate de execução não é evidência de competência para transformar intenção vaga em método executável.
2. **O gargalo é o procedimento, não a escolha.** Entre "qual abordagem" e "o procedimento completo e executável", é o segundo que desaba. Um gate de plano que aceita o *nome* de uma estratégia está entregando B2 e medindo B1: exija passos atômicos, artefato por passo, verificação declarada e condição de parada — ou aceite que o agente vai receber isso de alguém depois, informalmente, e ninguém vai contar.
3. **Correção automática do plano converte silenciosamente B3 em B1.** Quando um revisor conserta o brief e segue, a falha de formulação foi paga pelo harness e apagada do registro. Essa correção é dado de capacidade: tem de ser contada, endereçada e visível no veredito, não absorvida.
4. **Capacidade é modelo × harness; atribuição ao modelo sozinho é erro de medida.** Qualquer comparação entre executores precisa selar a configuração de inferência **efetiva** — a que de fato foi enviada —, não a declarada. Parâmetro descartado em silêncio no meio de uma chamada bem-sucedida é divergência de harness que o registro precisa carregar.
5. **A retirada de orientação tem de acontecer dentro da mesma tarefa.** Variar o rigor do ciclo por *tipo* de trabalho não mede autonomia: confunde dificuldade com grau de apoio. Se a pergunta é "este loop aguenta menos orientação?", a variação é sobre a mesma classe de unidade, com o resto constante.
6. **Diferença menor que a variância entre execuções não é achado.** Uma queda de 2,48 pontos ao lado de um desvio de ±2,35 é ruído; publicá-la dentro da frase "sharp decline" é o modo mais comum de um benchmark honesto produzir uma conclusão desonesta.

## Veredito do paper

Vale destilar, com o nome descartado na porta. O aparato transferível é pequeno e sólido: o gradiente de orientação sobre tarefa fixa, e a leitura correta de onde o desempenho quebra. As 60 tarefas de física computacional e astronomia não são terreno nosso e não vamos rodar nada disso; o que transfere é uma pergunta de desenho de gate que nenhum dos nossos pipelines faz hoje — *quanto do sucesso desta execução foi do executor e quanto do plano que alguém lhe deu?*

Não seguir: a moldura de superinteligência, os dois "primeiros", e qualquer comparação entre configurações separadas por menos de três pontos. O paper merece ser lido pela tabela 2 e pela frase sobre o gargalo ser procedimento; o resto do enquadramento é ruído de lançamento.

## Confronto com repos

### [[papers/repos-registry|agent-skills]]

1. **Teses aplicáveis**
   - **Tese 1 morde no osso.** `skills/issue-executor-master/SKILL.md:222` é regra dura: *"Nunca validar contra checklist fixo. A fonte da verdade é o brief aprovado a partir da issue."* Reforçada como anti-padrão em `SKILL.md:237` (*"a validação é sempre relativa ao brief aprovado"*) e como Regra 4 em `skills/issue-executor-master/references/fases-0-2.md:364-365` (*"O brief resultante desta fase é a única fonte de escopo das Fases 2–7"*). A regra está certa pelo motivo de segurança para o qual foi escrita — impedir que texto de issue amplie escopo (`SKILL.md:219`) —, mas tem um efeito colateral de medição: **as fases 2 a 7 rodam permanentemente em B1**, e nenhum gate do pipeline observa a qualidade da formulação.
   - **Tese 3 tem endereço exato.** `skills/issue-executor-master/references/fases-0-2.md:335`: *"Se encontrar gaps: CORRIGIR o brief automaticamente"*. O `issue-executor-brief-reviewer` acha a lacuna de formulação, tapa, e segue. Em `skills/issue-executor-master/references/fases-0-2.md:361-362`, `passed: false` após correções *"logar o motivo mas **não interromper** o fluxo"*, e `SKILL.md:197-200` generaliza: *"O fluxo **nunca bloqueia** por `uncertainty_score` alto ou `concerns`"*. Ou seja: a falha de plano é reparada pelo harness e o pipeline pontua a execução reparada.
   - **E o registro dessa correção é campo morto.** `SKILL.md:195` declara `corrections_applied[]`; `skills/issue-executor-master/references/fases-0-2.md:337` e `:437` mandam emiti-lo no JSON dos dois sub-agentes. `grep -rn "corrections_applied"` sobre o repo inteiro devolve **exatamente essas três linhas** — todas em texto de prompt. Nada lê, nada agrega, nada reporta. É o análogo do `retry_count` já diagnosticado em [[papers/deep/2026/08/2608.11341|17/08]]: campo declarado que mente por omissão.
   - **Tese 2 no template do brief.** `skills/issue-start/SKILL.md:235-236`: a seção `## Implementation Strategy` pede *"<Steps and the existing pattern to follow.>"* em prosa livre, sem artefato por passo e sem condição de parada. Ao lado, `## Validation Plan` (`:238-240`) exige comandos reais. O brief entrega, portanto, validação em nível B1 e estratégia em nível B2 — e é justamente o vão do B2 que o paper identifica como o caro.
   - **Tese 4 está meio-reconhecida.** `SKILL.md:187-190` acerta a metade difícil: *"esta skill **não pina modelo** [...] a skill deve ser testada no modelo em que vai rodar"*, com `subagent_model` em `.issue-executor.yaml`. Modelo é config de ambiente, e o repo sabe disso. A metade que falta é o registro: `scripts/run_harness.py:30-34` (`evaluator()`) sela `github-actions:run-<id>` ou `local:<host>` — **nunca qual modelo rodou**. O gate verde é atribuído à skill e à máquina; o par modelo×harness que produziu o resultado não sobrevive no `test-results.json`.

2. **Oportunidade de melhoria**
   Uma só, e é barata: **fazer o `corrections_applied[]` existir de verdade**. O sub-agente já computa a lista; ela morre no JSON de `artifacts/manifest/brief-review-$target_issue.json` (`skills/issue-executor-master/references/fases-0-2.md:359`) e não chega a lugar nenhum. Persistir a contagem e o texto das correções no relatório final da fase 7, ao lado do veredito, transforma o dado que hoje é apagado no único sinal de formulação que o pipeline tem. Zero token novo: é serializar o que já é gerado.
   Segunda, menor: acrescentar o modelo efetivo ao `evaluator()` de `scripts/run_harness.py:30-34`, lido da mesma cascata que `.issue-executor.yaml` já define. Sem isso, uma regressão de gate entre duas execuções é indistinguível de uma troca de modelo.

3. **Como aplicar**
   Ordem, uma issue cada:
   - **(a)** Propagar `corrections_applied[]` e `uncertainty_score` do `brief-review-$N.json` para o relatório da fase 7, como bloco informativo. Não bloqueia nada — `SKILL.md:223` proíbe bloquear por incerteza, e essa proibição fica de pé.
   - **(b)** Só depois de (a) ter uma dúzia de issues de corpus: separar o relatório em duas linhas explícitas — "brief precisou de N correções" e "execução contra o brief: PASS/FAIL" —, que é a tese 1 na forma mais barata possível.
   - **(c)** Modelo no `evaluator()`. Independente de (a) e (b), pode ir em paralelo.
   Nada de gate novo. O pipeline já carrega três sub-agentes e um loop de correção com teto 3 (`SKILL.md:226`); acrescentar bloqueio aqui compra cerimônia, não sinal.

4. **Prós e contras**
   Prós: (a) é dívida declarada — um campo especificado em três lugares e consumido em nenhum. O harness com três gates nomeados (`scripts/run_harness.py:87-94`) e a regra fail-closed (`:99-108`, gate sem teste é `passes = False`) já dão a disciplina para mexer sem medo. E o repo já entende modelo como configuração (`SKILL.md:187-190`), então (c) é completar um raciocínio que ele começou.
   Contras: o risco real é interpretar mal o próprio número. "N correções no brief" mede o brief *e* o revisor ao mesmo tempo — um revisor mais agressivo produz N maior sem que a formulação tenha piorado. O número só vira sinal se o revisor ficar estável, e ele não está pinado a modelo por decisão explícita (`SKILL.md:187-190`), o que é a tese 4 mordendo o próprio remédio. Segundo contra: `skills/issue-executor-master/references/telemetria.md:3-5` registra que `task-wrapper.sh` foi estopado pelo operador e que a skill não faz instrumentação manual; empurrar (a) longe demais reabre a discussão de telemetria que o operador já fechou.

5. **Veredito**
   **Aplicar com ressalvas.** Só (a) e (c) agora, ambos como registro sem gate. (b) fica condicionado a haver corpus que responda à pergunta. E a leitura do número nasce com a ressalva escrita junto: `corrections_applied` conta o par brief×revisor, não o brief.

### [[papers/repos-registry|agent-workloops]]

1. **Teses aplicáveis**
   - **Tese 1 já está implementada estruturalmente, e é o melhor exemplar que temos.** A Camada 1 (`playbooks/workloop.md:42-90`) é formulação de plano e tem gate próprio: `:71-75`, *"Um segundo agente critica o plano contra clareza, verificabilidade e completude, **antes de existir qualquer código**"*. A Camada 2 (`:94-176`) é execução, com o seu review adversarial separado (`:198-208`). Dois gates, dois momentos, dois avaliadores. O paper argumenta pela separação; este repo já a pratica.
   - **O que não existe é a ligação entre os dois.** O review de plano devolve aprovado/reescrever; o review de execução devolve *"findings com `file:line` e um veredito binário"* (`:202`). Nada correlaciona. O teto de duas rodadas (`:207-208`) já contém a intuição certa — *"se depois de duas correções o review ainda reprova, o problema não é a execução: é o desenho"* — mas é acionado uma vez e não vira série. A tabela de falhas observadas (`:305-312`) atribui seis falhas a mecanismos de prevenção, e **nenhuma** a qualidade de plano.
   - **Tese 2 está atendida no lugar certo.** `templates/diretiva.md:31-36` exige *"Passo atômico, com o artefato que produz"*, com verificação e review como passos explícitos, e `:10` declara que *"Seção vazia é sinal"*. Isso é B1 por desenho deliberado: a diretiva **é** o procedimento passo a passo cuja ausência derruba 21,82 pontos. A leitura honesta do paper contra este repo é que o workloop não tem um problema — ele tem uma proposta de valor, que é fornecer exatamente a orientação de que os modelos comprovadamente dependem.
   - **Mas os números do caso são B1-condicionados e não dizem isso.** `docs/caso-llm-council.md:26-37` traz a tabela: 15 issues, suite de 189 → 313 checagens, zero regressão, ~30 bloqueios do review, teto de rodadas acionado 1 vez. Todas as métricas pertencem à metade executora do arco descrito em `:13-24` — os passos 1 a 5 (investigação, interrogatório, plano, épico, contrato) não têm métrica nenhuma. E `:8-9` declara a intenção contrária: *"para que o playbook possa ser julgado por evidência e não por plausibilidade"*. A metade formuladora é julgada por plausibilidade.
   - **Tese 5 tem contraexemplo direto.** `playbooks/workloop.md:284-293`, "Aplicabilidade por tipo de trabalho", varia o rigor do ciclo por *tipo* de unidade — código no repo, artefato fora do repo, núcleo de alto risco, gate final. É variação de orientação **entre** tarefas diferentes, exatamente o confundimento que o desenho B1–B4 existe para evitar. Nada errado como política de custo; inútil como medida de autonomia.
   - **Tese 3 tem o análogo certo, aplicado a outro objeto.** `:102-103`: *"Divergência entre handoff e plano sobe ao operador, nunca é absorvida em silêncio"*, e `:218-219`, *"O plano é o que se pretendia; o handoff é o que aconteceu. Divergência entre os dois é sinal, não ruído."* A doutrina anti-absorção já existe e está bem escrita — só não alcança o caso "o revisor consertou o plano".

2. **Oportunidade de melhoria**
   Duas linhas de prosa, nenhum código (o repo não tem runtime: `playbooks/`, `docs/`, `templates/`).
   - Na §3.2 ou na tabela de verificadores (`:182-193`), declarar que o veredito do review mede execução **sob diretiva aprovada**, e que ele não é evidência sobre a capacidade de produzir a diretiva. Uma frase. É a única correção que impede o caso de origem de ser lido como prova de autonomia de agente.
   - No template do handoff (`templates/handoff.md`, cinco seções conforme `:212-214`), acrescentar à seção "decisões tomadas em voo fora do plano" a distinção entre decisão que o plano *deveria* ter previsto e decisão que só a execução revelava. A primeira é dado sobre a Camada 1; hoje as duas caem no mesmo balde e a Camada 1 nunca recebe retorno.

3. **Como aplicar**
   Um arquivo por vez, na ordem acima. Nada de acrescentar gate: `:195-196` já registra que sem CI o revisor adversarial é o único gate mecânico e é *"caro e falível"*, e `:233-248` ("Economia de gates") é o capítulo que diz o que **não** se delega — carregar mais no revisor vai contra a economia que o próprio playbook defende. As duas mudanças são de vocabulário e de escopo de afirmação, não de processo.

4. **Prós e contras**
   Prós: custo zero de migração, é markdown. O repo já sustenta quatro das seis teses em prosa — separação de gates (`:71-75` vs `:198-208`), procedimento passo a passo (`templates/diretiva.md:31-36`), anti-absorção de divergência (`:102-103`, `:218-219`), e o teto de rodadas como sinal de desenho (`:207-208`). Falta só nomear o que já se faz e cercar a afirmação do caso.
   Contras: o playbook vale por ser curto (328 linhas) e cada acréscimo o dilui. E há um risco de leitura: dizer "os números são B1-condicionados" pode ser lido como enfraquecer o caso, quando é o oposto — é a afirmação ficando do tamanho da evidência, que é a doutrina do próprio repo em `:225-229` (*"'Passou' sem endereço não é evidência, é asserção"*). Precisa ser escrito nesse tom ou vira autodepreciação.

5. **Veredito**
   **Aplicar.** Melhor razão custo/benefício dos três confrontos: duas frases de markdown fecham a distância entre o que o repo faz e o que ele afirma. A separação de gates já existe e é boa; o que falta é dizer em voz alta qual gate mede o quê. Não aplicar a tese 5 aqui — variar orientação sobre a mesma classe de issue custaria uma fase inteira de execução controlada, e o repo não tem volume para isso.

### [[papers/repos-registry|llm-council]]

1. **Teses aplicáveis**
   - **Tese 4 é a que morde, e morde no núcleo.** O conselho produz um ranking entre modelos por Borda cego, e apresenta esse ranking como comparação de modelos. Mas os membros **não rodam sob o mesmo harness**. `council.toml:10-11` declara para a OpenAI `max_tokens_field = "max_completion_tokens"` e `unsupported = ["temperature"]`; `council.toml:52` registra que a `temperature` fica fora do Claude de propósito (*"o Opus 5 devolve 400 se ela vier"*); `council.toml:53-54` que o thinking adaptativo do Opus 5 está ligado por padrão e consome do mesmo teto; `:45-46` e `:60-61` que DeepSeek e GLM são modelos de raciocínio, um deles já tendo gasto 8192 tokens só raciocinando. O `[settings] temperature = 0.3` de `council.toml:117` vale, na prática, para um subconjunto dos membros. `council/providers.py:157-158` e `:205-206` executam o descarte: `for campo in self.unsupported: payload.pop(campo, None)`.
   - **E a divergência de harness em tempo de execução é apagada quando dá certo.** `council/providers.py:274-284` (`_drop_offending_param`) reage a um 400 removendo o parâmetro ofensor ou renomeando `max_tokens` → `max_completion_tokens`. Em `council/providers.py:172-176`, o fato é anexado a `last` (*"[removido parametro 'X' e repetido]"*) e a chamada é repetida com `continue`; se a repetição funciona, `council/providers.py:168` retorna `self._parse(...)` e **`last` é descartado**. A string só vira `Reply.error` no caminho de falha (`:182-187`). Resultado: uma chamada bem-sucedida que rodou com configuração efetiva diferente da declarada não deixa rastro em lugar nenhum.
   - **O selo registra a config declarada, não a efetiva.** `council/provenance.py:76-99` (`config_snapshot`) é allow-list explícita e boa — grava `unsupported` por provedor (`:67-68`) e `params` por membro (`:82`), com o docstring em `:72` prometendo *"Config RESOLVIDA — inclui overrides de linha de comando"*. Mas "resolvida" ali significa resolvida **antes** da chamada. Duas execuções em que uma descartou `temperature` por 400 e a outra não produzem o mesmo `config_sha256` (`council/provenance.py:102-104`). O `Run` (`council/engine.py:104-142`), que já carrega 20+ campos aditivos, não tem onde guardar isso.
   - **Tese 5: o conselho só tem um nível de orientação, e é o máximo.** `council/prompts.py:15-43` (`ranking_prompt`) entrega ao avaliador os quatro critérios numerados de `council/prompts.py:7-12`, na ordem de peso, mais a instrução de penalizar resposta longa (`:31`) e o formato de saída literal (`:33-43`). Os perfis vão além: `council.toml:82-92` e `:99-109` nomeiam critérios *e* papel por conselheiro (*"cetico metodologico"*, *"cacador de premissa load-bearing"*, *"mapeador de dependencias"*). Não existe condição em que um membro precise formular o próprio critério de julgamento. Logo o Borda mede "quão bem este modelo aplica uma rubrica dada", nunca "quão bem este modelo julga" — e o repo não tem como distinguir as duas coisas.
   - **Tese 1, parcialmente, já tem instrumento.** `council/judgment.py:179-192` (`apurar()`, docstring literal: *"Taxa de concordancia entre o operador cego e o Borda"*) mede a segunda medida contra a primeira, com o cegamento montado em `council/judgment.py:91-109` (`cegar()`, que sorteia a ordem porque *"se a opcao 1 fosse sempre a favorita do Borda, o operador aprenderia o padrao"*, `:94-95`). É o único ponto do ecossistema onde uma medida independente é confrontada com o veredito oficial.

2. **Oportunidade de melhoria**
   Uma, cirúrgica: **selar a configuração efetiva por chamada**. Concretamente, fazer `council/providers.py:172-176` propagar o parâmetro descartado até o registro do membro em `Run.stage1` (`council/engine.py:112`), como campo aditivo do tipo `params_dropped: list[str]`. Hoje o dado existe por três linhas dentro de um `except` e é jogado fora no caminho feliz. Com ele, `provenance.compare()` (`:122-136`) — que já sabe reclamar de código mudado, commit diferente e árvore suja — ganha o terceiro tipo de divergência que hoje passa: harness que mudou **durante** a execução.
   Segunda, de prosa e não de código: `council.toml:32-34` justifica a diversidade de família (*"tres modelos do mesmo treinador erram junto e o ranqueamento vira teatro"*). Falta o parágrafo simétrico dizendo que o inverso também vale — quatro modelos sob quatro configurações efetivas de inferência não produzem um ranking de modelos, produzem um ranking de pares modelo×config. O comentário certo cabe ao lado do `[settings] temperature` de `:117`.

3. **Como aplicar**
   - **(a)** `params_dropped` como campo aditivo no dicionário de membro do estágio 1. É a regra do repo, escrita em `AGENTS.md:25` (*"Registros antigos em `runs/` jamais reescritos; campos novos são apenas aditivos"*) e praticada em `docs/prereg/2026-08-28-emenda-2.md` — exatamente como `resumed_from` e `stage2_mode` nasceram (`council/engine.py:137-142`). Cobertura pela suite offline `test_offline.py` (2623 linhas).
   - **(b)** O comentário em `council.toml` junto de `[settings]`, na mesma issue ou separado.
   - **(c)** Não aplicar a tese 5 por enquanto. Uma condição "sem rubrica" no estágio 2 é uma segunda chamada por membro por pergunta, num repo cujo `council cost` existe porque custo é restrição real (`council/cli.py:813`, *"ledger de custo dos registros e pre-voo de chamadas"*) e cujo `--rank-lite` (`council/cli.py:764`) foi criado justamente para *reduzir* o estágio 2. O custo é conhecido e a pergunta que responderia não está aberta.

4. **Prós e contras**
   Prós: (a) é o preenchimento de um buraco no selo de proveniência, que é o subsistema mais bem cuidado do repo — `council/provenance.py:1-9` já declara em prosa que *"Um registro sem isto e ininterpretavel depois de qualquer edicao"*, e essa frase se aplica inteira ao caso do parâmetro descartado. A disciplina de emenda pré-registrada torna a mudança de schema barata e auditável. E o custo em tokens é zero: nenhuma chamada nova.
   Contras: o caminho de `_drop_offending_param` é raro por construção (só dispara em 400 por parâmetro), então o campo vai passar a maior parte do tempo vazio — dado que raramente aparece é dado que ninguém lembra de olhar. Segundo contra: a divergência estrutural de harness (thinking adaptativo, `unsupported` declarado, modelos de raciocínio com orçamento próprio) é **maior** que a divergência de runtime que (a) captura, e (a) não a resolve; resolver de verdade exigiria equalizar as configurações, o que o `council.toml:8-9` explicitamente rejeita por custo medido em ida-e-volta de API. Há risco de (a) dar sensação de problema resolvido quando resolve a borda e não o miolo — que é por isso que (b) importa tanto quanto (a).

5. **Veredito**
   **Aplicar com ressalvas.** (a) e (b) juntos, nunca (a) sozinho: o campo sem o comentário conserta a borda e deixa a leitura errada de pé. A ressalva escrita na emenda é que o ranking do conselho é entre pares modelo×configuração, e que equalizar as configurações não está na mesa — é decisão de custo já tomada e documentada em `council.toml:8-9`. (c) não aplicar.
