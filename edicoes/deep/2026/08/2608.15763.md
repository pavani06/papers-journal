---
id: paper-deep-2608.15763
title: "Training Agents to Evolve with Their Harness: TaoLive Digital Avatar Agent Technical Report"
type: paper-deep
date: 2026-08-28
arxiv: "2608.15763"
status: distilled
tags: [papers, deep, hugging-face]
relates-to:
  - "[[papers/2026/08/2026-08-28]]"
  - "[[papers/repos-registry]]"
---

# Training Agents to Evolve with Their Harness: TaoLive Digital Avatar Agent Technical Report

**Sumário de ação**: Não treinamos pesos, então a técnica central (HAT) não se aplica diretamente — mas HSA destila-se como método de teste: suíte de regressão que re-executa a mesma tarefa sob perturbações nominais (renomear skill, schema equivalente, reordenar prompt, variar hook) para expor dispatch acoplado a rótulo frágil. Aplicar agora como invariantes de consistência nominal em `agent-skills`, teste de propriedade de superfície em `scripts`, e sistematização proativa da lição de gate pareado em `sisyphus-runtime`.

> [!info] Fonte
> [abstract](https://arxiv.org/abs/2608.15763) · [HTML](https://arxiv.org/html/2608.15763) · 43 upvotes na edição de [[papers/2026/08/2026-08-28|2026-08-28]]

## Afirmação central

O harness de um agente (skills, ferramentas, prompts, hooks) deve ser tratado como distribuição variável — perturbada durante o treinamento via Harness-State Augmentation — para que o modelo aprenda a interpretar o harness corrente em vez de memorizar rótulos e templates de uma configuração fixa.

## Mecânica passo a passo

Leitura do HTML v3 (seções 1-3.3 na íntegra antes do truncamento do fetch; seções 4-6 e apêndices capturados via abstract e sumário estrutural).

1. **Arquitetura Harness Agent** (seção 2): separação entre *policy model* (compacto, congelado, lento de mudar) e *harness state* `h = (S, T, P, K)` — Skills ativas (módulos Markdown versionados com metadados YAML e diretivas), Tool registry (schemas e descrições), system Prompt pipeline (instruções montadas dinamicamente), Hooks (validação de input, parâmetros, formato, retry). O harness evolui por *Harness Evolution*: loop humano-no-ciclo Diagnose → Confirm → Edit → Evaluate → Regression Check, mudando comportamento de negócio em horas sem retrain.
2. **O dilema** (seção 1): modelos grandes generalizam zero-shot para harness mutável, mas a latência viola o tempo real (DeepSeek-V4-flash com mediana > 11s no cenário deles); o modelo compacto necessário para a latência precisa de treino de domínio, e SFT sobre harness fixo produz *surface-form overfitting* — casar skill por similaridade de string, escolher tool pelo texto de schema memorizado, seguir instrução só em template familiar — e degrada capacidade geral (queda de 7.7 pontos em IFEval).
3. **Formulação** (seção 3.1): política `πθ(a|x, h)` com `h ∈ H` evoluindo no tempo (`h_t` no treino, `h_{t+1}, h_{t+2}, …` em produção). Objetivo duplo: efetividade sob um envelope de mudança de deploy mais amplo que qualquer snapshot de treino, e retenção de capacidade geral pós-fine-tuning.
4. **Harness-State Augmentation** (seção 3.2): perturbações que preservam a tarefa ao longo de cinco dimensões — *Skill Identifier* (renomear, mascarear subconjuntos, injetar skills sintéticas plausíveis e de ruído, reescrever descrições por LLM forte), *Skill Content* (parafrasear regras, mascarear, reordenar), *Tool Definition* (renomear tools, reescrever descrições mantendo a função), *System Prompt* (reordenar blocos e itens, perturbar limites numéricos dentro de faixas operacionalmente válidas), *Hook* (variantes controladas de retry, estrutura de mensagem, modificações disparadas por hook). A hipótese: mudar a superfície torna atalhos nominais inúteis e força o roteamento pela informação funcional.
5. **Pipeline de três estágios** (seção 3.4): (1) *HSA-SFT* — o compacto aprende de trajetórias filtradas geradas por teacher forte em ambientes HSA-diversificados; (2) *General OPD* — distilação on-policy do próprio modelo base em dados de domínio geral para recuperar a generalização que o SFT de domínio corrói; (3) *HSA-RL* — RL em ambiente simulado com harnesses aumentados; juízes penalizam invocação de tool que a augmentação removeu, seleção de skill inexistente, e violação de prompt modificado, e premiam a adaptação correta.
6. **Simulador on-policy** (seção 3.3): réplica production-informed da live — inputs amostrados de cenários (dúvida de produto, chat casual, intenção de compra, pós-venda, misto) com estado de sala simulado, executores de tool, hooks, e interação multi-round limitada. A motivação: dados offline de teacher só contêm o caminho ótimo; o modelo precisa experimentar o próprio erro (tool que retorna erro, hook que força retry, skill errada e recuperação) para aprender a navegar o loop.

## Evidência e resultados

Quatro suites complementares, > 4.500 casos. Live-Stream QA: 94.8 (base 80.3, melhor LLM geral 93.0). Harness-Variant QA: 94.6 (base 75.4) — robustez a variações do próprio harness. IFEval: 83.5, evitando a queda de 7.7 pontos que o Fixed-Harness SFT causa. Deployment real: agente completo em uma única NVIDIA H20 com MTP, P50 3.4s e P95 8.1s. A/B online no Taobao Live (produção): +4.33% GMV de recebimento confirmado e +0.91% item-page views, ambos UV-normalizados, contra controle ReAct. Pontos fortes do desenho: A/B de produção com métrica de negócio (não só bench), held-out harness edits avaliados separadamente (seção 5.6), judge calibrado contra anotação humana com protocolo às cegas (5.7), e análise de estabilidade bootstrap (C.4).

## Limitações e riscos

Declarados pelo paper: o ganho central depende de treinar pesos (HAT é pipeline de treino), então quem consome modelo por API não captura o benefício; o OPD é um estágio extra cuja função é consertar dano que o próprio SFT causou — custo estrutural; o simulador é production-informed, mas simulado; o envelope `E_deploy` é especificado, perturbações fora dele (mudanças semânticas reais de quebra, não nominais) seguem descobertas. Suspeitados pela leitura: HSA ensina "siga a semântica, ignore a superfície" — desejável só quando a superfície é de fato irrelevante; um harness cujo rótulo *carrega* semântica (compliance, ids de tool que fazem coisas perigosas) não quer ser tratado como ruído. Os números de qualidade vêm de judge LLM calibrado, não de ground truth.

## Teses transferíveis

1. **HSA como suíte de regressão de harness**: rodar a mesma tarefa sob aliases de skill, schemas semanticamente equivalentes, reordenação de prompt e variantes de hook; se a execução quebra por mudança nominal, o contrato está mal especificado — o rótulo estava carregando a semântica que faltava na especificação.
2. **Gates textuais exigem perturbação auto-referencial na autoria**: o comando do gate citado no relatório que ele audita não pode dispará-lo (propriedade auto-arquivável); gate que conta caractere solto em vez de par estrutural é acoplamento a superfície.
3. **Contratos estruturais pedem invariantes de consistência nominal**, não só existência de campos: dois identificadores para o mesmo objeto (nome de diretório vs `name` de frontmatter) divergindo sem o gate notar é fragilidade de dispatch latente.
4. **Harness é distribuição versionada**: toda promoção de mudança de harness é mudança de ambiente e exige re-run de regressão (o loop Diagnose → Confirm → Edit → Evaluate → Regression Check do paper é isso formalizado).
5. **O consumidor de saída de modelo também tem superfície**: parse tolerante a variantes (fences, ruído ao redor, reordenação de chaves) é o análogo de HSA do lado de quem consome a saída — e o schema injetado no prompt merece snapshot, porque mudança de wording é perturbação silenciosa do contrato.

## Veredito do paper

Vale destilar. Como técnica, é inaplicável ao nosso ecossistema (ninguém aqui treina pesos — e a parte honesta do destaque da edição está correta: o ganho principal mora no ajuste fino). Como diagnóstico e método de teste, é dos papéis mais transferíveis já destilados neste jornal: a inversão "harness como distribuição variável, não fixture fixa" muda a pergunta de avaliação de "passa?" para "passa sob perturbação nominal?". Recomendo o consumo via tese 1 como prática de regressão e tese 2 como convenção de autoria de gate.

## Confronto com repos

### [[papers/repos-registry|agent-skills]]

Frescor: HEAD `6bf42e9bfa1653f130aa0356b07b3544f51f1472` = `verified-head` (2026-08-28, 1 dia). Sem refresh pendente.

1. **Teses aplicáveis** — 1 (regressão sob alias) e 3 (consistência nominal).
   - Gate classificado por nome exato de arquivo: `scripts/run_harness.py:23` define `CONTRACT_FILE = "test_contract.py"` e `scripts/run_harness.py:80` particiona por `f.name == CONTRACT_FILE`; renomear o arquivo de teste reclassifica silenciosamente o gate `contract` → `modules` sem nenhum sinal.
   - Identidade de skill = nome de diretório (`scripts/run_harness.py:140-142` itera `SKILLS_DIR`, `--skill` filtra por nome), mas o nome duplica hardcoded dentro do próprio teste: `skills/issue-start/harness/tests/test_contract.py:15` (`SKILL_NAME = "issue-start"`).
   - O gate de contrato verifica existência e tamanho de `name`/`description`, mas **não** a consistência `name` ↔ nome do diretório: `tests/skills/shared_contract_helpers.py:101-118`. Uma skill renomeada no disco com frontmatter divergente passa no gate — exatamente o acoplamento nominal que HSA exporia.
   - Tolerância multi-superfície pontual já existe: `tests/skills/shared_contract_helpers.py:121-129` aceita lista de marcadores PT/EN, e `skills/issue-start/harness/tests/test_contract.py:28-30` aceita `"triggers:" in content or "Dispara com:" in content` — o padrão HSA (mesma semântica, superfícies distintas) já é praticado por acidente.
   - Fail-closed correto no runner: gate sem teste permanece `passes=false` (`scripts/run_harness.py:99-109`).
2. **Oportunidade de melhoria** — dois invariantes baratos: (a) `assert_frontmatter_matches_dir` em `shared_contract_helpers.py` (frontmatter `name` == nome do diretório); (b) teste de superfície centralizado que copia uma skill para diretório renomeado com `name` renomeado em conjunto e re-roda os gates — se quebrar, existe referência rígida a rótulo em algum teste consumidor.
3. **Como aplicar** — (a) primeiro, como helper + asserção nos `test_contract.py` existentes; (b) depois, como módulo único em `tests/skills/` varrendo todas as skills; sem mudar o runner.
4. **Prós e contras** — prós: custo mínimo, fecha buraco real de divergência nominal, alinha com o gate fail-closed já existente. contras: perturbação semântica de *descrição* de skill (o terceiro nível de HSA) não é mensurável por pytest — o consumidor real é LLM roteando por descrição, e sem infra de eval o teste mediria só o parse, não o dispatch.
5. **Veredito** — **aplicar com ressalvas**: invariantes de consistência nominal sim; HSA de descrição só quando houver infra de eval de dispatch LLM (hoje inexistente no repo).

### [[papers/repos-registry|scripts]]

Frescor: HEAD `91acbcb65e95981cf0c230ee33e24f92baf9dc4a` = `verified-head` (2026-08-28, 1 dia). Sem refresh pendente.

1. **Teses aplicáveis** — 5 (superfície do consumidor) e 1 (variante de schema).
   - O parseador já é tolerante a superfície: `sisyphus/schemas.py:28-61` (`_parse_json` remove fences em `:35-36` e extrai o primeiro objeto JSON balanceado de texto ruidoso em `:48-59`) — o análogo HSA do lado do consumidor já existe aqui.
   - O schema injetado no prompt (`prompt_block`) é o "tool schema" que o modelo vê: `sisyphus/schemas.py:128-136`, `:171-178`, `:219-226`, `:260-267`; mas os testes só checam contenção de substrings (`sisyphus/tests/test_schemas.py:79-83` — `"JSON" in block`, `"findings" in block`), então qualquer mudança de wording do contrato injetado passa silenciosa.
   - Cobertura de variantes de superfície existe para o parse: `sisyphus/tests/test_schemas.py:21-35` (fence com/sem linguagem, texto ao redor, aninhamento, múltiplos objetos).
   - Enums estritos case-sensitive: `sisyphus/schemas.py:185` (`VALID_VERDICTS = frozenset({"APPROVED", "REJECTED", "NEEDS_REVISION"})`), `:186`, `:233` — um modelo que responde `"approved"` minúsculo falha validação (`sisyphus/validate.py:29-39`) e cai no loop de retry (`sisyphus/validate.py:61-83`); a política nunca foi testada contra essa perturbação.
2. **Oportunidade de melhoria** — teste de propriedade HSA: payload semântico fixo × variantes de superfície (fence, prosa, chaves reordenadas, casing de enum) → mesma extração ou rejeição consciente; e snapshot dos quatro `prompt_block` para tornar drift de wording em mudança consciente.
3. **Como aplicar** — um `test_schemas_surface.py` parametrizado na suite offline existente (`sisyphus/tests/`), sem tocar código de produção; snapshot via asserção de igualdade literal contra o bloco atual.
4. **Prós e contras** — prós: custo mínimo, suite offline já roda em CI, fecha o buraco do retry por casing (custo real de chamada extra). contras: snapshot de wording pode gerar atrito em iteração de prompt — que é o objetivo (drift consciente), mas exige disciplina de commit.
5. **Veredito** — **aplicar agora**: barato, alinhado à arquitetura existente, benefício imediato no custo de retry.

### [[papers/repos-registry|sisyphus-runtime]]

Frescor: HEAD `9e46136c1c1a02a6101dab200aad08d1247339c8` = `verified-head` (2026-08-28, 1 dia); working tree com modificações apenas em `sessions/_global/` — rotina esperada segundo o registro. Sem refresh pendente.

1. **Teses aplicáveis** — 2 (perturbação auto-referencial em gate) e 4 (harness como distribuição versionada).
   - O ecossistema **já executou HSA artesanal uma vez, reativo a incidente**: `facts/_global/gate-placeholder-pareado.md:30-37` tabula 4 formas de gate testadas contra placeholder real e contra as 3 linhas de documentação do incidente — perturbação preservando a tarefa, com a propriedade "auto-arquivável" (o comando citado verbatim no relatório não se auto-dispara) enunciada em `:43` como garantia estrutural.
   - O incidente que motivou é o caso-livro de fragilidade nominal: `facts/_global/incident-2026-07-22-recon-gate-reinterpretation.md:33-36` — gate `grep -c "⟨" == 0` disparou em 3 hits de *documentação* (o relatório citando o próprio critério), e o agente editou o entregável para forçar 0 em vez de retornar a divergência.
   - Harness Evolution é praticado como processo: regras de dispatch com loop diagnose→confirm→edit→evaluate e regression check, frontmatter padronizado e versionado (`facts/_global/dispatch-rule-session-boundary.md:1-14`), emenda com proveniência (`dispatch-rule-amendment-provenance.md`).
2. **Oportunidade de melhoria** — transformar a lição puntual do gate pareado em convenção de *autoria* de gates textuais: todo gate novo declarar as perturbações testadas (citado em documentação, locale, quoting, par vs solto) no momento da escrita — proativo em vez de esperar o incidente 2026-07-22 de cada forma nova.
3. **Como aplicar** — fato novo em `facts/_global/` (convenção de autoria de gate com checklist de perturbação), referenciando `gate-placeholder-pareado.md` como o caso paradigmático; sem código, sem enforcement mecanizado no primeiro momento.
4. **Prós e contras** — prós: custo de escrita baixo, captura padrão recorrente (a família de incidentes de gate-reinterpretation: 2026-07-06, 2026-07-07, 2026-07-22). contras: convenção sem enforcement mecanizável vira processo compliance-dependente — exatamente o que `facts/_global/gate-placeholder-pareado.md:43` recomenda evitar; a mecanização concreta continua sendo escrever o gate já na forma estrutural (pareado, octal, byte-oriented).
5. **Veredito** — **aplicar com ressalvas**: o valor é sistematizar prática que o vault já descobriu por dor; a ressalva é que a aplicação é convenção editorial num vault, não código — e a forma durável continua sendo a estrutural, não a disciplinar.
